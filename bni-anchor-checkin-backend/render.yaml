services:
  - type: web
    name: bni-anchor-checkin-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: false
    healthCheckPath: /health
    envVars:
      - key: JAVA_OPTS
        value: -Xmx256m -Xms128m

