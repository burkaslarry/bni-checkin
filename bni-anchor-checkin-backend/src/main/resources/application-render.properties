# Render PostgreSQL - use with: --spring.profiles.active=render
# For Render deployment: set DATABASE_JDBC_URL, DATABASE_USERNAME, DATABASE_PASSWORD in Render env (or link DB for internal URL).
# Local dev: use defaults below or set env vars.

spring.datasource.url=${DATABASE_JDBC_URL:jdbc:postgresql://dpg-d6iok5q4d50c738643c0-a.oregon-postgres.render.com:5432/eventxp?connectTimeout=5&socketTimeout=10}
spring.datasource.username=${DATABASE_USERNAME:eventxp_user}
spring.datasource.password=${DATABASE_PASSWORD:i8WmAVN6TkTZlnaz3HKkttDITaVLptha}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=5000
spring.datasource.hikari.initialization-fail-timeout=-1

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
